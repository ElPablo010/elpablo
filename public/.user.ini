; Grote mp3-uploads (DJ-sets): PHP-limieten op Combell. Deze file wordt door
; deploy.sh mee gesymlinkt naar de /www-docroot en door PHP-FPM per map gelezen
; (cache ~5 min, user_ini.cache_ttl). Houd in sync met AudioPickerField
; (->maxSize) en config/livewire.php (temporary_file_upload.rules).
upload_max_filesize = 256M
post_max_size = 256M
